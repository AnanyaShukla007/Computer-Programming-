#include<stdio.h>
int main(void)
{
    int a,b,c,max;
    printf("Enter values for a and b and c: ");
    scanf("%d%d%d", &a, &b, &c);
    max =a > b ? (a > c ? a : c) : (b > c ? b : c) ;
    printf("Larger of %d, %d and %d is %d \n", a, b, c, max);
    return 0;
}
